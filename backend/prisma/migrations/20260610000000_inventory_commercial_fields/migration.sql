-- CreateEnum
CREATE TYPE "GstMode" AS ENUM ('INCLUSIVE', 'EXCLUSIVE');

-- AlterTable
ALTER TABLE "Product"
ADD COLUMN "sku" TEXT,
ADD COLUMN "discount" DOUBLE PRECISION NOT NULL DEFAULT 0,
ADD COLUMN "gstRate" DOUBLE PRECISION NOT NULL DEFAULT 5,
ADD COLUMN "gstMode" "GstMode" NOT NULL DEFAULT 'EXCLUSIVE',
ADD COLUMN "supplierPhone" TEXT NOT NULL DEFAULT '';
